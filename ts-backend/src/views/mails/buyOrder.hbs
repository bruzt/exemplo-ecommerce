<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">

    <title>Document</title>

    <style>
        * {
            margin: 0;
            padding: 0;
            outline: 0;
            box-sizing: border-box;
        }

        html,
        body {
            height: 100%;
        }

        body {
            font-family: Arial, Helvetica, sans-serif;
            background: #1C4061;
            color: #eee;
        }

        header {
            background: #0D2235;
            height: 100px;

            display: flex;
            justify-content: center;
        }

        header img {
            width: auto;
            max-width: 200px;
            height: auto;
            max-height: 100px;
        }

        main {
            width: 100%;
            min-height: 500px;

            padding: 0 30px;
        }

        main h1 {
            margin: 20px;

            text-align: center;
        }

        div.table-container {
            width: 100%;
            height: 100%;

            display: flex;
            flex-direction: column;
            justify-content: center;
            align-items: center;
        }

        main table {
            width: 500px;
            height: 100%;

            border-spacing: 0 5px;  
        }

        main table tr {
            margin-bottom: 5px;
        }

        main table td {
            text-align: center;

            overflow: hidden;
        }

        main table td.title {
            width: fit-content;
            max-width: 60%;
        }

        main table td.qtd {
            width: fit-content;
            max-width: 10%;
        }

        main table td.price {
            width: fit-content;
            max-width: 30%;
        }

        main .freight-price {
            margin-top: 50px;
        }

        main .freight-price strong {
            margin-top: 10px;
        }

        footer {
            height: 200px;
            background: #0D2235;
            display: flex;
            justify-content: center;
            align-items: center;
        }
    </style>
</head>

<body>
    <header>
        <a href="{{ website_url }}" target="_blank" rel="noopener noreferrer">
            <img src="http://qnimate.com/wp-content/uploads/2014/03/images2.jpg" alt="logo" />
        </a>
    </header>

    <main>
        <h1>Confirmação de compra</h1>

        <div class="table-container">
            <table>
                <thead>
                    <tr>
                        <th>Nome</th>
                        <th>Qtd.</th>
                        <th>Preço un.</th>
                    </tr>
                </thead>
                <tbody>
                    {{#each products}}
                        <tr>
                            <td class="title">{{this.title}}</td>
                            <td class="qtd">{{this.quantityBuyed}}</td>
                            <td class="price">R$ {{this.finalPrice}}</td>
                        </tr>
                    {{/each}}
                </tbody>
            </table>

            <div class="freight-price">
                <p>Frete: R$ {{freight}}</p>
                <strong>Preço total: R$ {{ totalPrice }}</strong>
            </div>
        </div>
        
    </main>

    <footer>
        <p>Contruido por Bruno Zutim</p>
    </footer>
</body>

</html>